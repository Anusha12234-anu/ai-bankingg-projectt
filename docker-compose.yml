version: '3.8'

services:
  backend:
    build: ./ai-app/backend
    ports:
      - "8000:8000"
    volumes:
      - ./ai-app/backend:/app
    environment:
      - DB_PATH=/app/ai_banking.db
    restart: always
